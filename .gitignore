# Build artifacts
/build/
/bin/
/jevonsd
*.o
*.a
*.d

# Compile database
/compile_commands.json

# IDE
.vscode/
.idea/
.cursor/

# Local scratch (screenshots, machine MCP map) — not product
/leftover-*.png
/jevons-root-chat.png
/old-cockpit-1440x900.png
/old-cockpit-a11y.md
/mcp.claudia.json
/node_modules/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# React cockpit (🎯T537.1 / T540) — source is tracked; install/build output is not.
# Dist is not go:embed (T360); daily GET / still serves web/index.html until T540.2.
/ui/node_modules/
/ui/dist/

# NOTE: //go:embed inputs must stay tracked (🎯T360) — a gitignored embed
# input makes a pristine clone fail `go build ./...` before any `make` run.
# internal/cli/help_agent.md is a committed mirror of agents-guide.md, kept
# in sync by the Makefile and ratcheted by scripts/docratchet.

# Xcode
/ios/Jevon.xcodeproj/
xcuserdata/
DerivedData/
*.xcworkspace

# Claude Code local settings. settings.json is TRACKED: it wires the shared-file
# write guard (🎯T376), which is fleet policy rather than a local preference.
/.claude/*

# Claudia exclusive-MCP isolate home (GROK_HOME / CODEX_HOME under workdir)
.claudia-mcp-home/
!/.claude/settings.json
sqlite_mcp_server.db

# Generated PlantUML output (regenerate via: plantuml -tsvg docs/*.puml)
docs/*.svg

# TLC model checker artifacts
formal/*_TTrace_*
formal/states/

# Swift package builds
swift/**/.build/
**/.build/
**/ModuleCache*/

