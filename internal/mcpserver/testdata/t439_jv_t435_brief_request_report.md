I did not receive your detailed brief — only the target itself and this message. Please resend it before I write anything.

(Read-only recon done meanwhile, so the resend can be terse: I've read `internal/server/reap_achieve.go` (the unlogged `reg.Remove` at :92/:97 plus the `slog.Info("auto-reaped work agents after target achieve")` in `maybeReapOnLedgerAchieve`), `internal/mcpserver/reap_done.go` (which *does* emit `logLifecycle(compAgentLifecycle, "reap_done", …)`), `internal/mcpserver/lifecycle_log.go`, `internal/server/browserlog.go` (`Server.LogEvent` → `eventlog.Log`), and enumerated the other registry-removal call sites: `internal/server/engagement.go:94,99` (stop), `internal/server/asides.go:308` (aside dismiss), `internal/mcpserver/lineage.go:117,121` (kill subtree), `internal/mcpserver/turn_evidence.go:469` (unbriefed seat), `internal/fleet/fleet.go:337` (butler remove). Open questions your brief probably answers: whether clause 1 wants a single accounted-removal choke point with a source-scanning guard test, and what surface clause 2 should use to make the reap legible to the parent PO.)